[package]
name = "rustdate"
version = "0.1.0"
edition = "2021"

[lib]
name = "rustdate"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.25", features = ["extension-module"] }
rayon = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
